services:
    _defaults:
        autowire: true
        autoconfigure: true

    App\MessageHandler\:
        resource: '../src/MessageHandler'
        tags: ['messenger.message_handler']

    App\:
        resource: '../src/'
        exclude: '../src/{MessageHandler,Entity,Migrations,Tests}'
